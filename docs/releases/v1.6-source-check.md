# v1.6 历史包与源码核对

此次补发使用原来的 `EchoSign-v1.6-win64.zip`，没有重新打包。文件大小为 555,630,013 字节，SHA-256 为：

```text
d127e02bd1ba0576891e320bd9bdd32041086fc0caf66d27fb723ecccca64cb8
```

历史构建时没有建立独立的版本提交。本次从保留的源码快照、原安装包、此前 Git 版本及本次工作的编辑记录恢复对应源码，再建立 `v1.6` 标签。

构建清单中 40 个源码、测试和资源文件与原始摘要逐字节一致。`echosign/gui.py` 的原始文本摘要未能恢复；恢复文本重新编译后，其全部 69 个代码对象、常量、变量名、异常表和行号信息均与原安装包内的 Python 代码一致。这里提供的是经编译结果核对的恢复文本，不宣称与原 GUI 文件逐字节相同。

清单中还有 3 个被忽略的测试运行日志，不属于源代码，未收录到标签中。`.gitignore` 沿用此前 Git 版本。本文件及[逐项核对记录](v1.6-source-provenance.json)是补发时新增的说明。

本次修复切页卡顿及直播稳定性的版本为 v1.7；v1.6 保留历史行为，供需要旧版的用户下载。
